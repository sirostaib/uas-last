class Department {
  late String dName;

  Department.copy({required this.dName});
  Department();
  // static Course fromMap(Map<String, dynamic> map) {
  //   return Course.copy(
  //       email: map['email'],
  //       cName: map['courseName'],
  //       cCode: map['courseCode']);
  // }
}
