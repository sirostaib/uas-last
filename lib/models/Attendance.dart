class Attendance {
  late String aDate;
  late String aStatus;

  Attendance.copy({required this.aDate, required this.aStatus});
  Attendance();
  // static Course fromMap(Map<String, dynamic> map) {
  //   return Course.copy(
  //       email: map['email'],
  //       cName: map['courseName'],
  //       cCode: map['courseCode']);
  // }
}
